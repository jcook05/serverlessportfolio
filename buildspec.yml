version: 0.1

phases:
   pre-build: 
     commands:
      - npm install
      - npm test
   build:
     commands:
      - npm run webpack
artifacts:
  files:
    - index.html
    - error.html
    - favicon.ico
    - John_Cook.pdf
    - styles/*
    - images/*
    - dist/bundle.js